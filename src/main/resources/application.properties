
## Spring DATASOURCE (DataSourceAutoConfiguration & DataSourceProperties)
spring.datasource.url=jdbc:mysql://localhost:3306/itsystemdb?useSSL=false
spring.datasource.username=jimboy02
spring.datasource.password=password

# The SQL dialect makes Hibernate generate better SQL for the chosen database
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL5InnoDBDialect

# Hibernate ddl auto (create, create-drop, validate, update)
spring.jpa.hibernate.ddl-auto=update
server.servlet.context-path=/itsystem

tokenSecret=dimple
server.error.include-stacktrace=never
springdoc.api-docs.path=/systeminfo


spring.application.name= its - service
